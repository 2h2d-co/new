# Agent Instructions

- This project is a TypeScript ESM CLI package.
- Keep the public CLI behavior documented in `README.md` when changing argument parsing or template semantics.
- Run `npm run check` before committing meaningful code changes.
